# Matchday Generation Rules

The canonical app instructions live in [`.instructions.md`](../../.instructions.md).
Use that file for the shared rules about naming, odds, stakes, cashout guidance, and persistence.

This file exists only to point agent work in `src/agents/**` back to the shared app definition.
